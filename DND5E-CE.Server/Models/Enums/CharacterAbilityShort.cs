namespace DND5E_CE.Server.Models.Enums
{
    public enum CharacterAbilityShort
    {
        STR,
        DEX,
        CON,
        INT,
        WIS,
        CHA,
        None  // Используем 'None' вместо '-' для валидного имени enum-элемента
    }

}
